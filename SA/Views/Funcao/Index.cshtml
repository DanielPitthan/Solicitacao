@model IList<SA.Models.Funcao>
@{
    ViewBag.Title = "Index";
}
<br />
@Html.ActionLink("Nova Funcao", "FormInclui", "Funcao")
<br />
<center>
    <table>
        <thead>
        <th>Id</th>
        <th>Funcao</th>
        <th colspan="2">Opções</th>        
        </thead>
        <tbody>
            <br />
            @foreach (var d in Model)
            {
                <tr>
                    <td>@d.Id</td>
                    <td>@d.Funcoes</td>
                    <td><a href=@Url.Action("FormAltera", "Funcao",new { id = d.Id })><i class="material-icons">mode_edit</i></a>@*@Html.ActionLink("Alterar", "Alter", "Funcao", new { id = d.Id },new { @class = "icon-mode-edit:before" })*@</td>
                    <td><a href=@Url.Action("Excluir","Funcao",new { id = d.Id })><i class="material-icons">delete_forever</i></a>@*@Html.ActionLink("Excluir", "Delete", "Funcao", new { id = d.Id })*@</td> 
                </tr>
            }
        </tbody>
    </table>
</center>